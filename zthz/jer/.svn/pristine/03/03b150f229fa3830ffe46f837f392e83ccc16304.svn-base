package qileke.jer.server.session;

import javax.servlet.http.HttpServletRequest;

import org.eclipse.jetty.server.session.AbstractSession;
import org.eclipse.jetty.server.session.AbstractSessionManager;

public class MemcachedSession extends AbstractSession{

	protected MemcachedSession(AbstractSessionManager abstractSessionManager, HttpServletRequest request) {
		super(abstractSessionManager, request);
		
	}

}
